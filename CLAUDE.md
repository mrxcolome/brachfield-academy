# CLAUDE.md — Brachfield Academy

Contexto operativo para sesiones de Claude Code. Leer antes de tocar código.

## Qué es este proyecto

Plataforma de membresía (39 €/mes, plan único) sobre Credit Management, prevención de impagos y recobro, de Pere Brachfield (perebrachfield.com). Público B2B profesional. Tres trabajos: **aprender** (cursos), **consultar** (buscador/herramientas), **actualizarse** (actualidad/eventos).

Prioridad de producto: `usabilidad > claridad > funcionalidad > diseño decorativo`. Idioma del producto y del contenido: **español**. Tono: profesional, claro, humano — nunca jerga tech ("Biblioteca", no "Content Library").

## Fuentes de verdad

1. **El briefing del producto** (100 secciones, en la conversación original / `docs/briefing.md` si existe) — manda sobre cualquier decisión técnica.
2. `TECHNICAL_PLAN.md` — arquitectura aprobada.
3. `DECISIONS.md` — ADRs; no revertir decisiones aceptadas sin nuevo ADR.
4. `ROADMAP.md` — fases; **no implementar funcionalidad de fases futuras**.

## Estado actual

- FASE 0 (Discovery) ✅ — ADRs aceptados el 2026-08-10.
- FASE 1 (Foundation) ✅ — Next.js 15 + TS strict + Tailwind 4, tokens portados, UI base, ESLint/Prettier/Vitest, CI.
- FASE 2 (Database) ✅ — schema aprobado, migración `init` validada contra Postgres 16, seed de usuarios demo, `src/lib/db.ts` (singleton con adapter pg). Neon creado (eu-central-1) pero INALCANZABLE desde el sandbox (red bloqueada): las migraciones a Neon se aplican con el workflow "DB migrate (Neon)" usando el secreto `DATABASE_URL` de GitHub.
- FASE 3 (Auth + Billing) ✅ código — Better Auth activo (signup/login/verificación/reset, rate limit), guards de 3 niveles (`src/features/auth/guards.ts`), Stripe Checkout + Customer Portal (server actions), webhook idempotente (`/api/webhooks/stripe`), EmailService con modo dev (sin RESEND_API_KEY imprime a consola), páginas auth/checkout/billing, middleware de /app. E2E verificado en local. VERIFICADO E2E en producción (2026-08-10): registro→verificación (Resend)→login→Stripe Checkout test→webhook→Subscription ACTIVE→acceso a /app, probado por el propietario con tarjeta 4242.
- DESPLIEGUE: dos proyectos Vercel sobre el mismo repo — `brachfield-academy` (Root Directory=prototype, la demo de diseño) y `brachfield-academy-app` (raíz, el producto real, https://brachfield-academy-app.vercel.app) con env vars: DATABASE_URL (Neon pooled), BETTER_AUTH__, NEXT_PUBLIC_APP_URL, RESEND_API_KEY (modo sandbox: solo entrega al email del dueño hasta verificar dominio), STRIPE__ (test mode; webhook en /api/webhooks/stripe).
- FASE 4 (Public Website) ✅ — layout público, landing 13 secciones, /pricing, /courses + /courses/[slug] (SSG), arte SVG portado (src/components/art), catálogo provisional tipado en src/features/content/catalog.ts (Fase 7 lo sustituye por Payload tras la misma interfaz), sitemap/robots/JSON-LD (FAQPage, Course), OG metadata, noindex en zonas privadas.
- FASE 5 (Onboarding) ✅ — /onboarding (4 pasos: bienvenida, perfil, objetivos multi, nivel), saveOnboarding con Zod, persistencia en User, flujo checkout/success→onboarding→/app (redirect si PENDING). Recomendaciones por reglas perfil→cursos en src/features/onboarding/recommendations.ts.
- FASE 6 (Application Shell) ✅ — layout /app con Sidebar (desktop), MobileNav (bottom, 5 tabs), Header (buscador global→/app/search, campana con badge de no leídas, UserMenu con logout), dashboard personalizado, centro de notificaciones con markAllRead, /app/account (editar perfil), búsqueda PROVISIONAL sobre catálogo (Fase 10 la sustituye por FTS), placeholders honestos en learning/explore/library/tools/updates/events/favorites, error.tsx + not-found.tsx, middleware sin dependencias Edge (warning eliminado).
- FASE 7 (Content Engine) ✅ — Payload CMS 3 embebido en /admin (requiere next@15.4, NO subir a 15.5 hasta que Payload lo soporte; package.json type=module). Colecciones: admins (auth propia del panel, roles admin/editor), categories, tags, media (disco local; adapter R2 pendiente de cuenta Cloudflare), contents (11 tipos, drafts+autosave+schedulePublish, transcript, relatedContent, SEO), courses (módulos→lecciones como arrays anidados ordenables), events. Schema Postgres `payload` con migraciones propias (src/migrations, la inicial crea el schema). Seed editorial en src/payload/seed.ts (idempotente, BORRA contenido; SEED_DEV_ADMIN=1 crea admin dev admin@brachfieldacademy.test/brachfield-dev-2026 SOLO en dev). CI valida payload migrate + seed. Workflow Neon ejecuta payload migrate y seed editorial opcional (input seed_editorial). En prod el primer usuario del panel se crea en /admin (pantalla create-first-user).
- FASE 8 (Courses) ✅ — src/lib/cms.ts (Local API singleton), src/features/content/service.ts (getPublishedCourses/getCourseBySlug/flattenLessons), src/features/learning/ (progress.ts puro + service.ts + actions.ts markLesson/saveLessonPosition con validación de pertenencia). /app/learning real (en progreso/por empezar/completados), /app/courses/[slug] (ficha con temario y ✓), /app/courses/[slug]/[lessonId] (sidebar temario, render Lexical vía @payloadcms/richtext-lexical/react, transcript, prev/next, botón completar optimista), dashboard con "continúa donde lo dejaste" real. PENDIENTE de Cloudflare: player de vídeo (placeholder en lecciones video/audio); "Recomendado para ti" aún usa el catálogo estático (se cambia a Payload en Fase 13).
- FASE 9 (Library) ✅ — servicio de contenidos ampliado (getPublishedContents con filtros tipo/categoría/nivel, getContentBySlug depth:1, getFeaturedContents, getContentsByIds, CONTENT_TYPE_META/LEVEL_META), /app/explore (destacados, temas, cursos, novedades), /app/library (filtros por query params con chips-enlace, sin JS cliente), /app/contents/[slug] (ficha genérica: player placeholder según formato, RichText, transcript, relacionados depth:1, botón Guardar), Favoritos REALES (src/features/favorites: actions toggleFavorite server-side resolviendo el id desde el CMS, service + resolve.ts puro testeado, /app/favorites con chips por formato usando contentType desnormalizado), ContentCard compartida. Seed editorial ahora marca 4 featured y relatedContent manual (briefing §80) — en PROD el editor puede marcarlos con la casilla featured en /admin sin re-seedear (re-seedear BORRA y recrea contenido → cambia ids y rompe favoritos/progreso existentes).
- FASE 10 (Search) ✅ — SearchService (src/features/search/service.ts): FTS Postgres con to_tsvector('spanish')+unaccent EN TIEMPO DE CONSULTA (sin índice GIN: catálogo pequeño; añadirlo en migración futura cuando crezca) sobre payload.contents (título A, subtítulo/excerpt B, transcripción C) y payload.courses (título A, descripción B, títulos de lecciones C), websearch_to_tsquery (input del usuario seguro), fallback a subcadena unaccent+ILIKE si el FTS no devuelve nada ("buro"→"burofax"), y logSearch → tabla search_query (solo búsquedas sin filtro, nunca rompe la búsqueda). Migración 20260811125641_search_unaccent (CREATE EXTENSION unaccent; el workflow Neon la aplica al hacer push). /app/search real con chips de formato (Cursos + tipos presentes en resultados). Tests de integración en tests/integration/search.test.ts — en CI corren DESPUÉS del seed editorial de Payload (orden cambiado en ci.yml).
- FASE 12 (Events) ✅ (adelantada a la 11 por decisión del propietario 2026-08-11: la 11 espera la cuenta Cloudflare) — src/features/events/: service.ts (lecturas CMS: upcoming/replays/bySlug), reservations.ts (SOLO Prisma, separado para que los tests de integración no carguen Payload: reserve con control de aforo no-atómico aceptado y documentado, cancelReservation, counts), format.ts (fechas Europe/Madrid "hora peninsular"), actions.ts (guards + email de confirmación no bloqueante). Migración event_reminder_sent_at (reminderSentAt anti-duplicados; OJO tras migrar: npx prisma generate). /app/events real (próximos con aforo restante + ReserveButton optimista, replays → /app/contents/[slug]). Recordatorios: /api/cron/event-reminders (Bearer CRON_SECRET, idempotente, verificado E2E en local) + vercel.json cron diario 07:00 UTC. PENDIENTE del propietario: crear env var CRON_SECRET en Vercel (sin ella el cron responde 401 y no se envían recordatorios). Emails nuevos en src/emails/templates.ts (eventReservedEmail, eventReminderEmail). Seed: 3 eventos (2 futuros + 1 pasado con replayContent).
- FASE 11 (Tools + Cloudflare) ✅ código (2026-08-11; cuenta Cloudflare del propietario creada) — @payloadcms/storage-s3 activo SOLO con env vars R2_* (dev sigue en disco local; el sandbox no alcanza R2). Descargas: src/features/tools/downloads.ts (downloadUrlFor: URL firmada R2, attachment 5min / inline 3h para audio; fallback dev a media.url), actions.ts requestDownload (guard + DownloadLog), /app/tools real (contador de descargas, badge "más usada"), DownloadButton en fichas. SEGURIDAD: media.access.read — con R2 activo solo imágenes públicas; documentos/audio SOLO por URL firmada. Player: StreamPlayer (subdominio de cuenta customer-rwzj2lpoidmf4nvf.cloudflarestream.com — las cuentas actuales de Stream NO reproducen por iframe.videodelivery.net, aprendido 2026-08-17; el subdominio es público y vive como constante en stream-player.tsx) en lecciones (rawLesson.streamId) y fichas VIDEO/WEBINAR; audio con <audio> y URL firmada inline. Seed: 4 PDFs de marca en src/payload/seed-assets/ subidos como media y vinculados como documentFile. Vars Vercel que el propietario añadió: R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, CLOUDFLARE_STREAM_TOKEN (este último aún sin uso en código: reservado para signed playback / subida vía API). PENDIENTE verificar en prod: subida de media a R2 desde /admin y un vídeo real en Stream (pegar su UID en streamId). CLIENT UPLOADS (2026-08-12): Vercel corta peticiones >4,5 MB ("Your request was too large") → s3Storage con clientUploads:true (navegador→R2 con URL prefirmada). REQUIERE regla CORS PUT en el bucket (LAUNCH_CHECKLIST 1b, pendiente del propietario). OJO importmap: el plugin solo se activa con las vars R2_* — regenerarlo con dummies (R2_ACCOUNT_ID=dummy … npx payload generate:importmap) para que incluya S3ClientUploadHandler.
- FASE 13 (Personalization) ✅ — src/features/personalization/: rules.ts PURO (scoreItem: +3 categoría afín al perfil, +2 interés del onboarding cubierto, +1 nivel; rankItems desempata por publishedAt; mapas PROFILE_CATEGORIES e INTEREST_CATEGORIES sobre los NOMBRES de categoría del seed) + service.ts (getRecommendations: 1 curso top no completado ni en curso + contenidos top, OJO consultas con depth:1 — con depth:0 las categorías son ids y todo puntúa 0, bug ya sufrido; getNewThisWeek: publishedAt > now-7d). Dashboard real: recomendaciones desde Payload, "Próximo directo" = primer evento futuro con enlace a /app/events, "Nuevo esta semana" con glyphs. Seed: COURSE_CATEGORIES por curso (antes todos 'Credit Management'). BORRADO src/features/onboarding/recommendations.ts (+su test), sustituido por personalization.
- FASE 14 (Admin) ✅ — Zona /app/admin (guard requireRole('ADMIN','EDITOR'); usuarios SOLO ADMIN) con tabs: Resumen (KPIs: miembros activos, altas 30d, descargas 30d, reservas, preguntas pendientes + búsquedas top y SIN RESULTADOS desde search_query = huecos editoriales), Usuarios (tabla con búsqueda, estado de suscripción, RoleSelect optimista — un admin no puede cambiarse su propio rol, COMPANY_ADMIN se muestra pero no se ofrece), Preguntas (cola PENDING/SELECTED con responder→notificación SYSTEM al autor / seleccionar para Q&A / descartar; la INTAKE de miembros "Pregunta a Pere" es V1 — la cola ya está lista), Eventos (reservas/aforo por evento). src/features/admin/{service,actions}.ts — service solo Prisma (resolveQuestion testeado en tests/integration/admin-questions.test.ts). Sidebar muestra "Administración" solo a ADMIN/EDITOR (layout /app pasa showAdmin). Enlace "Abrir el CMS ↗" en la cabecera del admin.
- FASE 15 (Analytics) ✅ código — src/features/analytics/service.ts: track() SOLO server-side (fetch directo a PostHog /capture/, host UE; sin SDK en navegador → sin cookies de tracking → sin banner por analítica), no-op sin POSTHOG_API_KEY (dev imprime console.debug), NUNCA lanza. 17 eventos tipados (union AnalyticsEvent): user_signed_up (databaseHook de Better Auth), checkout_started, subscription_activated/canceled + payment_failed (billing/service.ts detecta TRANSICIONES comparando status previo antes del upsert; webhooks), onboarding_completed, lesson_completed + course_completed (markLesson cuenta done==total), content_viewed (ficha RSC), search_performed/search_no_results (logSearch), tool_downloaded, favorite_added/removed, event_reserved/event_reservation_canceled, question_answered. Regla briefing §45: NUNCA PostHog directo en componentes — siempre track(). ACTIVO EN PROD (confirmado 2026-08-28: proyecto 246532 en eu.posthog.com recibiendo eventos; POSTHOG_API_KEY ya en Vercel). Los eventos van identificados solo por userId (cuid) sin nombre/email — enriquecer con $set es decisión pendiente del propietario (implicaciones RGPD/política de privacidad). video_played y pageviews cliente: V1 (requeriría SDK js + consentimiento).
- CAMBIO del propietario (2026-08-11) ✅: eliminado "nivel" de los CURSOS (colección+migración Payload 20260811_231858 que DROPea la columna, admin, UI pública y privada, seed, búsqueda con NULL::text, personalización con level:null). Los contenidos SÍ mantienen nivel. El workflow db-deploy ahora también se dispara con src/migrations/**. OJO aprendido: payload migrate se quedó colgado por una fila 'dev' en payload.payload_migrations (del next dev local) — borrarla si vuelve a pasar; y npm run start sirve el código COMPILADO: cambios en src requieren next build antes de re-testear.
- FASE 16 (Testing) ✅ — Playwright E2E: 19 tests / 9 flujos críticos en tests/e2e (guards, signup→verify→login→paywall, onboarding 4 pasos, curso+progreso+dashboard, búsqueda FTS, biblioteca+favoritos, descarga con download_log, eventos reservar/cancelar, admin gating+responder pregunta) + accesibilidad axe (wcag2a/aa, violations serious/critical = fail; halló contraste text-faint en ContentCard → text-muted). Setup: global.setup.ts (project dependency) crea usuarios @e2e.brachfieldacademy.test vía API (¡con cabecera origin — Better Auth exige Origin!) + SQL con pg directo (tests/e2e/utils/db.ts), guarda storageState member/admin en tests/e2e/.auth (gitignored). Rate limit: sign-up tiene regla interna 3/10s de Better Auth → el webServer de playwright.config arranca con AUTH_RATE_LIMIT_DISABLED=1 (SOLO tests). CI: job e2e nuevo (playwright install chromium, build, test:e2e, sube test-results si falla). Local: npm run test:e2e (usa /opt/pw-browsers/chromium). docs/SECURITY_REVIEW.md: revisión de seguridad completa con 3 pendientes aceptados.
- FASE 17 (Performance) ✅ — getCoursesProgress (1 consulta para el progreso de todos los cursos, fix N+1 de /app/learning), React.cache en getCourseBySlug/getContentBySlug/getCategories (dedupe generateMetadata+página), HSTS añadido (cierra pendiente #3 del SECURITY_REVIEW). Medidas en docs/PERFORMANCE.md: First Load JS 101kB, público SSG (TTFB 3-7ms), /app ~240-320ms local. DECISIÓN: sin unstable_cache del CMS (catálogo pequeño, riesgo de contenido rancio) y sin GIN aún. OJO E2E: si un servidor manual quedó corriendo sin AUTH_RATE_LIMIT_DISABLED, playwright lo reutiliza y falla el spec de signup — pkill next-server antes.
- FASE 18 (Production) ✅ — Sentry (@sentry/nextjs) gated por env: src/instrumentation.ts (register solo runtime nodejs, tracesSampleRate 0.1, onRequestError) + src/instrumentation-client.ts (NEXT_PUBLIC_SENTRY_DSN, solo errores) + src/app/global-error.tsx (captura + pantalla digna en español); sin DSN todo es no-op. Branding del panel Payload (pedido del propietario): src/payload/branding.tsx (Logo/Icon PB navy+gold) vía admin.components.graphics + generate:importmap. Staging = Vercel preview deployments (⚠️ comparten env de prod — documentado). docs/LAUNCH_CHECKLIST.md: pasos del propietario (Sentry DSN, dominio, Resend domain, Stripe live, Vercel Pro, contenido real, legales). MVP COMPLETO: 18/18 fases.
- LOGO OFICIAL (2026-08-12) ✅: SVG entregados por el propietario en public/brand/ — logo.svg (positivo, fondos claros) y logo-neg.svg (negativo, fondos oscuros: cuadrado blanco + "Academy" naranja); icon.svg/icon-neg.svg son SOLO el símbolo (mismos rects, encuadre recortado) para favicon y espacios pequeños. REGLA del propietario: el logo va SIEMPRE tal cual, sin inventar colores ni estructuras. PALETA OFICIAL definitiva 2026-08-20 (2ª decisión del propietario, tras ver la preview granate): AZUL #172B49 = marca y acción (confianza/educación; OJO: más oscuro que el navy antiguo #224278); GRANATE #A21E26 y NARANJA #E88800 = detalles y acentos puntuales (los colores del logo; token --color-garnet, usado p. ej. en las flechas del proceso de la landing); NEGRO #000000 = tinta sobre papel cálido (tokens ink neutros, sin matiz azul). LOGO nuevo: icono granate+naranja idéntico en ambas versiones; nombre negro en el positivo y blanco en el negativo (logo-neg.svg derivado del positivo oficial solo cambiando el color del nombre, según muestra visual del propietario; icon-neg = icon). Tokens en src/styles/globals.css; arte SVG en src/components/art. src/components/brand/logo.tsx (BrandLogo/LogoMark, prop variant, sirven los archivos con <img>) se usa en cabecera pública (26px móvil / 38px desktop), pie, sidebar de /app y layout de (auth); el panel Payload (src/payload/branding.tsx) alterna positivo/negativo según html[data-theme]; favicon en src/app/icon.svg.
- IMÁGENES LANDING (2026-08-16) ✅ INTEGRADAS: 22 imágenes IA para las tarjetas de la landing (10 formatos + 8 áreas + 4 recursos), generadas vía MCP de Magnific (conector claude.ai del propietario) con Seedream 5 Pro, 16:9, 2K, y la imagen "formato-cursos" aprobada como referencia de estilo de toda la serie. TODAS aprobadas por el propietario el 2026-08-16. Mapa identificador→nombre de archivo en docs/landing-images.json. Integradas el 2026-08-16: el sandbox no alcanza el CDN de Magnific (pikaso.cdnpk.net) ni con la política de red actualizada (solo aplica a contenedores nuevos), así que se usó el workflow .github/workflows/fetch-landing-images.yml como puente — se dispara al cambiar docs/landing-image-urls.json (URLs firmadas temporales, retiradas tras el uso), descarga en el runner, optimiza con sharp a WebP 1600px q82 y commitea public/landing/*.webp [skip ci]. Las tarjetas usan next/image con el campo img del catálogo (whatsInside/knowledgeAreas/sampleTools). Para REGENERAR alguna imagen: MCP Magnific → images_generate (seedream-5-pro, 16:9, 2k, referencia estilo 79NMEd9JAL) → actualizar docs/landing-images.json → volver a poblar docs/landing-image-urls.json con la URL fresca y push. PENDIENTE ADEMÁS de la landing: captura real del dashboard sustituyendo <DashboardMock> (bloque central), y 3 fotos REALES de Pere que debe pasar el propietario (hero 4:3, avatar 56px de la cita, "Sobre Pere" 1:1) — NO generarlas con IA.
- PORTADAS EN 3 NIVELES (2026-08-16) ✅: campo coverImage (upload→media) en contents y courses (migración 20260816_173220_add_cover_images, aplicada en local; a Neon vía workflow db-deploy). Resolver en src/features/content/covers.ts: (1) coverImage subida por el editor → (2) foto de la serie de marca por formato (DEFAULT_COVER_BY_TYPE, 11 tipos) o por área del curso (COVER_BY_CATEGORY por NOMBRE de categoría del seed; fallback formato-cursos) → (3) arte SVG como red final (SmartCover en src/components/product/smart-cover.tsx). Aplicado en ContentCard, dashboard (getContinueLearning ahora devuelve cover y usa depth 1), learning, explore, search (SQL sin coverImage: solo nivel 2 por formato — hueco conocido y aceptado), ficha de curso, y páginas públicas /courses y /courses/[slug] (campo img en PublicCourse del catálogo estático). OJO: las lecturas de content/service.ts pasaron de depth 0 a depth 1 para resolver coverImage — si se añade una relación pesada a esas colecciones, revisar el coste. Tests del resolver en tests/unit/covers.test.ts. La captura public/landing/dashboard.webp se regeneró con el look nuevo.
- INCIDENTE PRODUCCIÓN RESUELTO (2026-08-17): /app daba 500 ("Failed query: select courses…"). CAUSA RAÍZ: una fila 'dev' en payload.payload_migrations de la BD de producción (Neon, ep-jolly-mouse) hacía que `payload migrate` del workflow db-deploy saliera VERDE SIN APLICAR NADA desde el principio — la BD se quedó en la migración inicial y estalló con la primera migración que AÑADÍA una columna que el código selecciona (cover_image_id). Guardas dejadas: (1) el workflow retira la fila 'dev' antes de migrar (scripts/remove-dev-migration-row.mjs), (2) paso "Verificar columnas de portadas" (scripts/verify-cover-columns.mjs) que pone el workflow en ROJO si la BD no queda como el código espera — un verde ya no puede mentir, (3) migraciones remove_course_level y add_cover_images reescritas idempotentes (IF EXISTS/IF NOT EXISTS), (4) el seed de usuarios demo del disparo manual es opt-in (seed_demo_users). Diagnóstico que lo destapó: endpoint temporal /api/health/db (host enmascarado + columnas + migraciones + error real de la consulta) — RETIRARLO al cerrar el incidente. Aprendizajes: los logs de runtime están en Vercel → Logs; GitHub Actions y api.github.com son inalcanzables desde el sandbox (los logs de workflows los pasa el propietario); Vercel y el workflow SÍ comparten la misma BD.
- DOMINIO DEFINITIVO CONECTADO (2026-08-28) ✅: **https://brachfieldacademy.com** (Cloudflare Registrar; CNAME @ y www → Vercel con nube gris, www y .vercel.app redirigen 308 al raíz). Resend verificado (emails a cualquier destinatario, remitente `EMAIL_FROM=Brachfield Academy <hola@brachfieldacademy.com>`); NEXT_PUBLIC_APP_URL y BETTER_AUTH_URL al dominio nuevo (OJO: si BETTER_AUTH_URL falta o no coincide con el origen, TODO signup devuelve 403 [Better Auth] — sufrido en el estreno); webhook Stripe → dominio nuevo; CORS R2 con ambos orígenes. Emails con logo oficial (public/brand/email-logo.png servido desde el dominio, generado de logo.svg con sharp). El enlace de verificación deja al alumno en /checkout (paso 2) con sesión iniciada (callbackURL en signUp.email + autoSignInAfterVerification). «Enable Receiving» de Resend apagado a propósito; respuestas a hola@ → Cloudflare Email Routing (pendiente opcional).
- ACTIVIDAD DE USUARIOS Y EDITORES (2026-08-28) ✅ (pedido del propietario): (1) login de alumnos → databaseHooks.session.create de Better Auth escribe User.lastLoginAt + evento user_logged_in con person $set (nombre/email visibles en PostHog — decisión RGPD del propietario; reflejar en política de privacidad); (2) columna «Último acceso» + botón «Eliminar» (action deleteUser: solo ADMIN, no a uno mismo, bloqueado con suscripción vigente) en /app/admin tab Usuarios, y FICHA por alumno (/app/admin/users/[id]: cronología logins/búsquedas/descargas/favoritos/progreso/reservas; datos de service.ts solo-Prisma + títulos CMS en features/admin/activity.ts); (3) actividad EDITORIAL: tabla editorial_activity (migración editorial_activity) alimentada por hooks Payload (src/payload/activity.ts: afterLogin en admins + afterChange/afterDelete en contents/courses/events/media — los UPDATE de borrador/autosave NO se registran, solo create y guardados publicados, para no inundar) + tab «Editores» (solo ADMIN) con resumen por editor y actividad reciente; (4) SEGURIDAD: colección admins del CMS ahora con access create/delete solo admin del panel (antes cualquier editor podía crear/borrar usuarios del panel), update solo admin o la propia ficha. Eliminar editores del CMS = nativo de Payload (Usuarios del panel → seleccionar → Delete), ahora solo para administradores.
- SALA DE PROFESORES · FASE 1 (2026-09-03) ✅: capa guiada sobre Payload para crear cursos sin manual (proyecto consensuado en el doc de trabajo «Sala de profesores», artifact 57386488; DECISIONES del propietario 3/09: subida directa de vídeo EN FASE 1, Cristina publica sin aprobación, avisar (no bloquear) al borrar lecciones con progreso, sin SEO en la Sala, solo cursos, y SIN MÓDULOS: estructura Curso→Lecciones con UN módulo técnico invisible HIDDEN_MODULE='Contenido' — cero migración, cursos multi-módulo del CMS quedan solo-CMS hasta la fase 2). Implementación: src/features/sala/ (service.ts: Local API de Payload con overrideAccess, slug único, lexical mínimo texto↔richText, totalDuration al publicar, logSala escribe editorial_activity+PostHog firmando con el usuario de la APP — los hooks de Payload no ven req.user desde la Local API y por diseño no duplican; actions.ts con Zod+requireRole('ADMIN','EDITOR')). UI en /app/sala (home con listado y estados, /nuevo, /[id]?paso=1..4 con SalaSteps): asistente de 4 pasos, autogardado por operación, aviso de progreso al borrar (borrarLeccion devuelve warning y pide confirm). VÍDEO DIRECTO: tus-js-client → POST /api/sala/stream-upload (proxy de creación tus a api.cloudflare.com/...stream?direct_user=true con CLOUDFLARE_STREAM_TOKEN+R2_ACCOUNT_ID; los chunks van navegador→Cloudflare, cabecera stream-media-id devuelve el UID) + estadoVideo() rellena duración al procesarse. OJO sandbox: api.cloudflare.com inalcanzable — el circuito de subida está verificado salvo la llamada real (probar en prod con un vídeo). Sidebar: entrada «Sala de profesores» para ADMIN/EDITOR. Recorrido E2E completo verificado con Playwright en local (crear→lecciones→reordenar→publicar→retirar).
- CATÁLOGO POR CONCEPTOS (2026-09-03) ✅ (decisión del propietario: el alumno busca promesas, no formatos; Excel compartido con Pere): tipos de contenido ahora TUTORIAL (nuevo), PILL (nuevo, «Píldora»), INTERVIEW (nuevo, «Entrevista» — sustituye a AUDIO/Podcast, que era formato y no promesa; migración 20260903_113609), WEBINAR (etiqueta «Sesión en directo»), GUIDE, CHECKLIST, TEMPLATE, CASE_STUDY, NEWS. RETIRADOS del vocabulario: VIDEO→TUTORIAL, PDF→GUIDE, ARTICLE→GUIDE, TOOL→TEMPLATE, AUDIO→INTERVIEW (el formato es un ATRIBUTO: streamId/audioFile/documentFile; la ficha muestra player/audio/descarga según lo que HAY, sea el tipo que sea — y pueden combinarse). Migraciones 20260903_112701_rename_content_concepts y 20260903_113609_rename_audio_to_interview (recrean los enums de contents y _contents_v CON UPDATEs de mapeo previos — sin ellos el cast `USING ::text::` falla con valores retirados — y migran también public.favorite.contentType desnormalizado; PATRÓN a repetir si se renombra otro tipo). Tocados: contents.ts (opciones; condiciones por tipo de streamId/audioFile/documentFile ELIMINADAS — cualquier pieza puede llevar cualquier material), CONTENT_TYPE_META (9 conceptos), covers (TUTORIAL→formato-videos, PILL→formato-recursos PROVISIONAL — regenerar imagen de píldoras con Magnific, INTERVIEW→formato-podcasts), tools TOOL_TYPES=[TEMPLATE,CHECKLIST], landing whatsInside (10 conceptos) + FAQ + BENEFITS, checkout/signup INCLUDES, seed, eventos etiqueta, tests covers. La sección «Herramientas» del alumno agrupa plantillas+checklists.
- SALA DE PROFESORES · FASE 3 «PIEZAS SUELTAS» (2026-09-03) ✅: la Sala ya crea las 10 tipologías — el curso (asistente de fase 1) y las 9 piezas sueltas (contents). /app/sala/contenido/{nuevo,[id]?paso=1..4}: paso 1 «La pieza» (selector visual de los 9 conceptos con glifo+pista, título, dos frases para la tarjeta, área, nivel opcional), paso 2 «El material» (vídeo directo reutilizando VideoUpload/tus + UID manual, texto plano→lexical, archivo descargable pdf/doc/xls y audio — COMBINABLES; publicar exige al menos uno), paso 3 portada opcional (sin ella entra la automática del concepto), paso 4 checklist+publicar/retirar con «Verla como alumno» → /app/contents/[slug]. Código: src/features/sala/concepts.ts (SALA_CONCEPTS client-safe — OJO: no importar desde service.ts en componentes cliente, lleva `server-only`), service.ts+actions.ts ampliados (SalaContent, crear/guardar/material/archivos/publicar; subidas vía server action con tope 4 MB — límite de Vercel; audios largos → modo experto o entrevista en vídeo), WizardSteps genérico en steps.tsx, home de la Sala con dos estanterías (Cursos / Piezas sueltas) y «+ Crear una pieza». logSala registra en editorial_activity con collection 'contents'. Recorrido E2E verificado con Playwright (checklist con texto+PDF real creada y publicada).
- REDISEÑO ZONA DE ALUMNOS (2026-09-09) ✅ (aprobado sobre la maqueta artifact fcb64d93 «Zona de alumnos v2»): (1) MENÚ de 8→6: Inicio, Mi formación, Explorar, Actualidad, Favoritos + **«Nuevo este mes»** al final, separada y en NARANJA (tokens nuevos --color-accent-strong #a85c00 texto ~5:1 sobre blanco — el naranja puro del logo da 2,7:1 e incumple AA — y --color-accent-soft #fdf2e2 activo; glifo ✦ en accent puro); móvil: pestaña «Perfil» sustituida por «Nuevo» (la cuenta sigue en el UserMenu de la cabecera). Biblioteca/Herramientas/Eventos fuera del menú (URLs vivas). (2) EXPLORAR = escaparate en retícula con jerarquía: Cursos 2×2 (→/app/learning, cuenta real), 4 medianas con foto (Tutoriales/Píldoras/Entrevistas→library?tipo=X, Sesiones en directo→updates), 5 compactas con glifo (Guías/Casos→library, Checklists/Plantillas→/app/tools, Actualidad→updates), Destacados + áreas; imágenes = /landing/formato-*.webp (las de la landing). (3) ACTUALIDAD (/app/updates) real: «El sector, al día» (contents NEWS), «Próximas sesiones en directo» (reservas con ReserveButton, absorbe Eventos) y Replays; **/app/events → redirect a /app/updates** (emails antiguos siguen funcionando). (4) **/app/new «Nuevo este mes»**: histórico cronológico por meses (contents+courses por publishedAt, Europe/Madrid). (5) ETIQUETA NUEVO global: src/features/content/fresh.ts (isNew ≤30 días, test unit) + NewBadge en ContentCard (y feeds de updates/new). (6) INICIO: bloque «¿Qué necesitas hoy?» (Aprender/Resolver/Ponerte al día → learning/library/updates), cajón «Nuevo este mes» (getNewThisWeek(3, 30) — ahora acepta days) con «Ver todo lo nuevo →». (7) Renombrado alumno: «Eventos»→«Sesiones en directo» (colección Payload labels incl.), tab admin «Eventos»→«Directos», /app/tools retitulada «Plantillas y checklists». E2E 07/08 adaptados (heading nuevo + redirect). PENDIENTE V1 pactado: email mensual «Este mes en la Academia» (diseñar aparte antes de enviar nada).
- PRIMER ATERRIZAJE DEL ALUMNO (2026-09-09) ✅ (aprobado sobre la maqueta, tableros «Aterrizaje» y «Tour»): Inicio ADAPTATIVO — src/features/welcome/ (rules.ts puro isWelcomeActive: bienvenida hasta completar «Tus primeros pasos» o 14 días del alta, test unit; service.ts getWelcomeState calcula la checklist SIN tablas nuevas: curso=userProgress>0, favorito, reserva de directo, y «ver el vídeo de Pere» solo si hay vídeo; actions marcarTourVisto/marcarVideoBienvenidaVisto). Migración Prisma welcome_experience (User.tourSeenAt + welcomeVideoSeenAt). UI: src/app/app/welcome-home.tsx (bienvenida-escaparate: intro, WelcomeVideo, checklist autocompletable, «Diez maneras de ayudarte» con jerarquía desde src/features/content/tiles.ts — módulo COMPARTIDO con Explorar —, y al cierre «Empieza por aquí» = curso de getRecommendations con fallback, o «Continúa» si ya hay progreso). TOUR: src/components/product/welcome/nav-tour.tsx (6 paradas por selector sobre el menú real — buscador, Mi formación, Explorar, Actualidad, Nuevo este mes, Favoritos — overlay + anillo naranja + Siguiente/Anterior/Saltar; en móvil tarjetas centradas sin foco; al cerrar marca tourSeenAt); se abre solo la primera vez y se reabre con /app?tour=1 (enlace «Volver a ver el tour» en Tu cuenta). VÍDEO DE PERE: src/features/welcome/config.ts WELCOME_VIDEO_STREAM_ID='' — PENDIENTE que Pere lo grabe (1 min, móvil horizontal); al pegar el UID el bloque pasa de tarjeta de texto a reproductor y aparece el paso en la checklist. Verificado E2E con Playwright (tour→cerrar→persistencia→?tour=1→favorito marca la checklist→alumno de 20 días ve el Inicio de trabajo).
- TABLÓN «EL SECTOR, AL DÍA» (2026-09-09) ✅ código (pedido del propietario: actualidad del nicho automática y curada, «como si Pere colgara noticias»): src/features/sector-news/ — sources.ts (RSS de Expansión/Cinco Días/El Economista/Europa Press; URLs NO verificables desde el sandbox (red bloqueada): se estrenan con el botón del admin, que reporta el estado POR FUENTE — corregir ahí las que fallen y añadir BOE/CEPYME cuando se confirmen sus canales), parse.ts (parser RSS mínimo puro, test), relevance.ts (filtro por palabras clave del nicho, red de seguridad, test), service.ts (refreshSectorNews: fetch tolerante por fuente → curación con Claude API `claude-opus-5` — @anthropic-ai/sdk, editor que elige ≤6 titulares y escribe resumen de 1 línea; sin ANTHROPIC_API_KEY o si falla → keywords sin resumen → upsert por url única en tabla sector_news (migración Prisma sector_news) + poda >45 días). Cron diario /api/cron/sector-news (Bearer CRON_SECRET, vercel.json 05:30 UTC) + action actualizarTablon (ADMIN/EDITOR) con botón «Actualizar el tablón ahora» en el Resumen del admin (muestra curación usada y titulares/errores por fuente). UI: sección «El tablón del sector» en Actualidad — titular + resumen + fuente·fecha, ENLACE SIEMPRE A LA FUENTE (target _blank; resumir y enlazar es legal, republicar no). Jerarquía: las piezas Actualidad de Pere arriba, el tablón debajo; empty state solo si no hay ninguno. Pendiente del propietario: ANTHROPIC_API_KEY y CRON_SECRET en Vercel (sin la primera el tablón funciona en modo keywords). V2 VISUAL (9/09, tras retirar el REDACTOR «gemelo de Pere» por decisión del propietario — publicar crónicas firmadas con IA «no es creíble»; writer.ts BORRADO): la sección «El sector, al día» es ahora RETÍCULA VISUAL — 1-2 noticias destacadas AL DÍA (MAX_SELECTED_PER_RUN=2, muy selectivo), histórico cronológico SIN poda, hero grande + tarjetas 2 col. Cada noticia: imagen del RSS (parse extrae media:content/enclosure/<img>; NewsImage client con onError → rotación estable de fotos de marca), sello HOY, etiqueta temática (NEWS_TOPICS), titular grande → fuente (_blank), «La clave para ti» (la frase didáctica de la curación: por qué le importa a un credit manager) y «En la academia: …» (la curación elige contenido relacionado del catálogo por índice — relatedSlug/Title/Kind en sector_news, migración sector_news_visual). Las piezas Actualidad manuales de Pere quedan debajo como «El análisis de la academia». maxDuration=60 en cron y admin (las llamadas a Claude superan los 10s por defecto de Vercel).
- ACTUALIDAD = SOLO NOTICIAS (2026-09-10) ✅ (decisión del propietario al ver la retícula: «me cargaría las sesiones en directo… y dejaría solo las noticias»): /app/updates queda SOLO con la retícula «El sector, al día» + «El análisis de la academia» (columna única max-w-4xl, sin grid 2fr/1fr). **/app/events RESUCITADA como página propia «Sesiones en directo»** (ya NO redirige): Próximas con reserva + Replays. Recolocados todos los caminos → /app/events: tile «Sesiones en directo» de Explorar/aterrizaje (tiles.ts), «Reservar plaza» del cajón «Próximo directo» del Inicio, paso «directo» de la checklist de bienvenida (welcome/service.ts), y el paso Actualidad del tour reescrito (solo noticias, remite a Explorar para los directos). E2E 08 vuelve a esperar heading «Sesiones en directo» en /app/events. Esto CORRIGE el punto (3) del rediseño del 9/09 (Actualidad ya no absorbe eventos).
- ESTADO FINAL MVP: todas las fases del roadmap completadas (2026-08-12). Trabajo restante = pasos del propietario del LAUNCH_CHECKLIST + V1 del ROADMAP. Playbook del propietario publicado como artifact de Claude (actualizarlo si cambia algo gordo) + PDF de herramientas en el chat.
- El prototipo vive en `/prototype/`. Deploy de Vercel: hasta la Fase 4, la demo pública es el prototipo — al hacer push del restructure hay que poner Root Directory=`prototype` en los ajustes de Vercel.
- Desarrollo en sandbox: Postgres 16 local (service postgresql start; BD brachfield_dev, user dev/dev).

## Stack (una vez aprobados los ADRs)

Next.js 15 App Router + TS strict + Tailwind 4 · PostgreSQL (Neon) + Prisma · Payload CMS 3 embebido (`/admin`) · Better Auth · Stripe (Checkout + Portal + webhooks idempotentes) · Cloudflare Stream (vídeo) + R2 (audio/docs) · Postgres FTS tras `SearchService` · PostHog (analytics + flags) · Resend + React Email · Sentry · Vitest + Playwright · next-intl (es).

## Convenciones

- **Dominios en `/src/features/<dominio>/`** (auth, billing, content, courses, learning, search, favorites, events, tools, notifications, analytics). Nada de lógica en `utils.ts`/`helpers.ts` genéricos.
- Mutaciones = **Server Actions** con schema **Zod** + guard de autorización (`requireUser` / `requireActiveMember` / `requireRole`). Toda autorización es server-side; ocultar UI no autoriza nada.
- Fechas en **UTC** en BD; render en timezone del usuario.
- Textos de UI en `messages/es.json` (next-intl) — no hardcodear strings dispersos.
- Acceso premium: validar SIEMPRE contra `Subscription.status` local (sincronizado por webhooks Stripe).
- Eventos analytics solo vía `AnalyticsService.track()` — nunca PostHog directo en componentes.
- REGLA del propietario (2026-08-20): tipografía MÁS GRANDE en móvil — base html de 17px bajo 640px en globals.css (público sénior); al añadir textos, preferir clases rem (text-sm, text-base…) sobre px arbitrarios para que escalen.
- REGLA del propietario (2026-08-20): en MÓVIL los bloques se EDITAN, no se encogen — menos texto que en desktop (se ocultan párrafos secundarios y píldoras decorativas con hidden sm:block) y el orden puede cambiar (p. ej. imagen del hero arriba). No clonar el desktop en pantallas pequeñas.
- REGLA del propietario (2026-08-16): NUNCA limitar el ancho de párrafos con max-w estrechos que provoquen saltos de línea "bruscos" (el texto se parte pronto dejando vacío a la derecha). Los párrafos fluyen al ancho de su contenedor; max-w solo para elementos estructurales (contenedores, tarjetas, acordeones).
- Commits pequeños y claros; cambios estructurales grandes se explican ANTES de ejecutarlos.
- TS strict, sin `any`; tipos generados desde schema (Prisma/Payload) cuando existan.
- Una funcionalidad no está terminada sin: tipado, validación, autorización, loading/error/empty states, responsive, accesibilidad y tests de lo crítico (§97 del briefing).

## Definición de "hecho" por fase

Al cerrar cada fase: tests pasan, `npm run build` limpio, documentación afectada actualizada (README/ARCHITECTURE/DATABASE/ENVIRONMENT/DEPLOYMENT), resumen de cambios al propietario, y siguiente paso propuesto.

## Forma de trabajar con el propietario

Es el product owner; no asumas conocimientos técnicos profundos. Para cada fase: (1) objetivo, (2) qué se construye, (3) decisiones necesarias → preguntar, (4) implementar, (5) tests, (6) errores, (7) resumen, (8) docs, (9) siguiente paso. Ante un problema: problema → impacto → alternativas → recomendación. Sin sobreingeniería: monolito modular; nada de microservicios/Kafka/K8s.

## Comandos

```bash
npm run dev           # Next.js dev server (http://localhost:3000)
npm run build         # build de producción
npm run typecheck     # tsc --noEmit
npm run lint          # eslint
npm run format:check  # prettier
npm test              # vitest (tests/unit)
npm run db:migrate    # prisma migrate dev (Postgres local)
npm run db:seed       # seed de usuarios demo
# prototipo: cd prototype && npm run dev
```

## Git

- Remote: https://github.com/mrxcolome/brachfield-academy (deploy automático de `main` a Vercel).
- El entorno remoto de Claude NO tiene credenciales de push permanentes: pedir al propietario un fine-grained PAT (Contents: Read and write) cuando haya que subir, o que instale la GitHub App de Claude con write.
